# Served by Cloudflare Workers static assets (requests matching files in
# dist/ never reach the worker, so headers must be set here, not in src/index.js).
# Vite content-hashes everything under /assets/ — safe to cache forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
